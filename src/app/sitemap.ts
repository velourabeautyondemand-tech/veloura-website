/**
 * This file is disabled to resolve a conflict with src/app/sitemap.xml/route.ts.
 * Next.js dynamic sitemaps can conflict with manual route handlers.
 * We are using the manual route handler for byte-level control over output.
 */
export const dynamic = 'force-dynamic';
export const revalidate = 0;
